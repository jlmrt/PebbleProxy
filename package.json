{
  "name": "pebble-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "A privacy-first Pebble gateway for Umbrel",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development ROLE=all node --watch src/server.js",
    "test": "node --test --test-concurrency=1",
    "check": "node scripts/check.mjs"
  }
}
